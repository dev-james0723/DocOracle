# Input Directory

Place your PDF file here before running the pipeline.

**Example:**
```
input/
  └── my_textbook.pdf
```

Then run:
```bash
export GEMINI_API_KEY="your-key-here"
bash pipeline/run_all.sh
```

**Important:** PDF files in this directory are gitignored to protect copyright. Only place PDFs you have legal rights to use.
